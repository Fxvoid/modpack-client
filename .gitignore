# 1. Ignore everything in the repository by default
*
!.gitignore

!

# 2. Allow specific *essential* top-level folders to be tracked
!/config/
!/defaultconfigs/
!/kubejs/
!/mods/

# 3. Allow *all contents* recursively within those essential folders
!/config/**
!/defaultconfigs/**
!/kubejs/**

# Handle the 'mods' folder separately: only track .jar files, ignore all else at root of /mods
!/mods/*.jar 

# 4. Now, EXPLICITLY RE-IGNORE the specific client-side user configs
# These files are *within* /config/, which was previously unignored by !/config/**,
# but Git's more specific ignore pattern for these exact files should now take precedence.
/config/distraction_free_recipes-client.toml
/config/fabric/
/config/jade/
/config/jei/
/config/oculus.properties
/config/packetfixer.properties
/config/compasscoords-client.toml

# Also explicitly ignore this large mod file
/mods/L_Enders_Cataclysm-3.16.jar

!update_mods.bat
!update_server.bat
!Client_Side_Only_Mods.txt